- Codex message generation uses its non-interactive exec command. If an AI provider fails, generation can fall back to the next installed provider.
- AI messages use a bounded excerpt of the working-copy diff.
- Merge conflict loading avoids redundant work.
